- hosts: all
  become: true 
  tasks:
    - name: create local user account on the target server
      user:
        name: john
        shell: /bin/bash
        append: yes
        generate_ssh_key: yes
        ssh_key_type: rsa
        ssh_key_file: .ssh/id_rsa_john
